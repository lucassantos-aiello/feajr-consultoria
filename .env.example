# Copy to .env.local for local testing (vercel dev). NEVER commit real values.
# In production these live in Vercel → Settings → Environment Variables.

PIPEFY_TOKEN=your_pipefy_personal_access_token
PIPEFY_PIPE_ID=123456789
